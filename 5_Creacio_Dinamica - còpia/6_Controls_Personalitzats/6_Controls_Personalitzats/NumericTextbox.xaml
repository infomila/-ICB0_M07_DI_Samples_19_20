<UserControl 
    x:Name="NumericTextBox"
    x:Class="_6_Controls_Personalitzats.NumericTextbox"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:_6_Controls_Personalitzats"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400"
    HorizontalAlignment="Stretch"
    Loaded="UserControl_Loaded"
             
    >
    <!--TextBox x:Name="txbNumero" HorizontalAlignment="Stretch" 
             KeyDown="txbNumero_KeyDown"
             Text="{Binding ElementName=NumericTextBox, Path=Text,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" 
             VerticalAlignment="Center"></TextBox-->
    <TextBox x:Name="txbNumero" HorizontalAlignment="Stretch" 
             KeyDown="txbNumero_KeyDown"      
             TextChanged="txbNumero_TextChanged"
             VerticalAlignment="Center">
    </TextBox>
</UserControl>
