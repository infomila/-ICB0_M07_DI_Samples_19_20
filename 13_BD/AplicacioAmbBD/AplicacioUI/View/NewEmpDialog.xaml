<ContentDialog
    x:Class="AplicacioUI.View.NewEmpDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AplicacioUI.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400"
    Loaded="ContentDialog_Loaded">

    <Grid
            Grid.Row="2"
            >
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>

        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>

        </Grid.RowDefinitions>

        <local:UIFitxaEmp x:Name="uiFitxa" Grid.Column="0" Grid.Row="0"></local:UIFitxaEmp>

        <CommandBar Grid.Column="0" Grid.Row="1" Margin="10">
            <AppBarButton Icon="Save" Click="btnSave_click"  x:Name="btnSave" Label="Save"  />
            <AppBarButton Icon="Cancel" Click="btnCancel_click"   x:Name="btnAdd" Label="Cancel"   />
        </CommandBar>

     
    </Grid>
</ContentDialog>
